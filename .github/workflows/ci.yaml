name: Test

on:
  issues:
    types:
      - opened
      - edited

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ISSUE_CONTEXT: ${{ github.event.issue }}
    steps:
      - run: |
          python3 main.py
